<!-- این فایل برای کپی‌پیست مستقیم در بخش About/Description مخزن Docker Hub است.
     به‌روزرسانی خودکار: python .freebuff/hub_description.py (نیازمند docker login با رمز حساب) -->

# پلتفرم ارزیابی عملکرد کارکنان (HR KPI Platform)

سیستم جامع ارزیابی عملکرد (KPI) برای منابع انسانی — فارسی، تقویم شمسی، راست‌به‌چپ.

## ✨ قابلیت‌ها
- **داشبورد مدیریتی** با نمودار روند، رادار پروفایل، اسپارک‌لاین تیم‌ها و شمارنده‌های متحرک
- **نمای کلی تیم‌ها** — مقایسه میانگین KPI همه تیم‌های تحت مدیریت کنار هم (برترین/پایین‌ترین نفر، زیر ۶۰)
- **امتیازدهی دستی + اکسل** — خروجی اکسل برای مدیر تیم، ورود مجدد برای به‌روزرسانی
- **فرمول میانگین وزنی** با وزن‌های قابل‌تنظیم برای هر تیم + ترکیب ارزیابی ۳۶۰ و اهداف
- **دسترسی‌های دانه‌ریز تیک‌بی‌تیک** — مدیر چندتیم، کارمند با مجوز نمره‌دهی خاص
- **احراز هویت JWT** با ۴ نقش: مدیر سیستم، مدیر منابع انسانی، مدیر تیم، کارمند
- **طرح بهبود عملکرد (PIP)** خودکار برای عملکرد پایین دو دوره متوالی
- **خودارزیابی** با مقایسه در گزارش نهایی | **ارزیابی ۳۶۰ درجه** بین هم‌تیمی‌ها
- **گزارش‌های سفارشی** با فیلتر تاریخ، خروجی اکسل و PDF سمت سرور (فارسی/شمسی)
- **مقایسه دو دوره** کنار هم برای هر کارمند
- **تقویم شمسی** در همه‌جا — yyyy-mm-dd، ساعت زنده، پیکر‌های شمسی
- **تیم‌ها، کارمندان، انتقال، آرشیو** | **ورود گروهی** از JSON/اکسل
- **پشتیبان‌گیری** دستی + خودکار شبانه (نگهداری ۳۰ روز) | **گزارش حسابرسی**
- **تاریخچه نسخه‌ها** جمع‌شونده در صفحه «درباره»

## 🚀 اجرا

```bash
docker run -d -p 8000:8000 meysam8498/hr-kpi-platform:backend-latest
docker run -d -p 3000:3000 -e NEXT_PUBLIC_API_URL=http://localhost:8000 meysam8498/hr-kpi-platform:frontend-latest
```

یا با Docker Compose (پیشنهادی) — [docker-compose.yml در گیت‌هاب](https://github.com/meysam8498/hr-kpi-platform/blob/main/docker-compose.yml):

```bash
git clone https://github.com/meysam8498/hr-kpi-platform
cd hr-kpi-platform
docker compose up -d
```

## 👤 حساب‌های پیش‌فرض
| کاربر | نقش | رمز اولیه |
|---|---|---|
| `admin` | مدیر سیستم | `admin123` |
| `hr` | مدیر منابع انسانی | `hr123456` |

⚠️ در اولین ورود تغییر رمز اجباری است.

## 🏗️ استک فنی
FastAPI + SQLAlchemy + Alembic | Next.js 15 + TypeScript | SQLite (WAL) | Openpyxl + fpdf2

## 📄 لایسنس
MIT — © 2025 [Meysam Ijadi](https://github.com/meysam8498)
