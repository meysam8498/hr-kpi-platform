# 🎯 سیستم مدیریت KPI (KPI Management System)

یک سیستم کامل و **محلی** برای مدیریت عملکرد و ارزیابی KPI کارمندان، ساخته‌شده با **React/Next.js**، **Python/FastAPI** و **SQLite** — بدون وابستگی به سرویس‌های ابری، قابل اجرا با **Docker**.

- **فرانت‌اند:** Next.js (React 19) — پورت پیش‌فرض `8003`
- **بک‌اند:** FastAPI — پورت پیش‌فرض `8000`
- **دیتابیس:** SQLite (حالت WAL) — مناسب تا ~۵۰ نفر
- **ورود:** بدون احراز هویت — یک ادمین کل سیستم را مدیریت می‌کند

---

## 📋 فهرست مطالب

- [امکانات](#امکانات)
- [شروع سریع (Docker)](#شروع-سریع-docker)
- [راه‌اندازی توسعه محلی](#راه‌اندازی-توسعه-محلی)
- [معماری و جریان داده](#معماری-و-جریان-داده)
- [مرجع API](#مرجع-api)
- [فرمول محاسبه KPI](#فرمول-محاسبه-kpi)
- [راهنمای پیکربندی KPI](#راهنمای-پیکربندی-kpi)
- [ساختار پروژه](#ساختار-پروژه)
- [طراح سیستم](#طراح-سیستم)

---

## امکانات

| بخش | قابلیت‌ها |
|------|-----------|
| 👥 **مدیریت کارمندان** | افزودن، ویرایش، حذف، **انتقال بین تیم‌ها**، **آرشیو** (مخفی از نمای فعال)، **ورود گروهی** (اکسل یا JSON) |
| 🏢 **مدیریت تیم‌ها** | ساخت تیم، افزودن/حذف عضو، مشاهده اعضا |
| 📊 **امتیازدهی KPI** | ورود دستی نمره (۰-۱۰۰) برای هر معیار + **خروجی/ورودی اکسل** (دانلود فرم، آپدیت نمره‌ها) |
| ⚖️ **پیکربندی KPI** | معیارهای مشترک و تخصصی، وزن‌دهی به تفکیک تیم، **ترکیب نمره نهایی** (نمره مدیر + ارزیابی ۳۶۰ + اهداف) |
| 📅 **دوره‌های گزارش‌گیری** | ساخت دوره (ماهانه/فصلی/…)، فعال‌سازی، **آرشیو خودکار** دوره‌های بیش از ۳ ماه |
| 🧮 **موتور محاسبه KPI** | میانگین وزنی + ترکیب با ارزیابی همکاران و تحقق اهداف؛ نتایج کش‌شده و قابل محاسبه مجدد |
| 📈 **داشبورد منابع انسانی** | نرخ ترک خدمت، توزیع نمرات، رتبه‌بندی تیم‌ها، روند دوره‌ها، برترین‌ها، **فاصله خودارزیابی/مدیر**، **هشدار افت متوالی**، **نرخ غیبت**، **تحقق اهداف** |
| 📊 **گزارش‌ها** | گزارش تیمی و فردی، نمودار روند (Recharts)، **چاپ/PDF** |
| 🔔 **اعلان‌ها** | زنگ اعلان با نشانگر، یادآوری مهلت دوره‌ها، صفحه کامل اعلان‌ها |
| 🎯 **اهداف (OKR-lite)** | تعریف هدف کمی برای هر کارمند، پیگیری پیشرفت، درصد تحقق |
| ◬ **ارزیابی ۳۶۰** | هم‌تیمی‌ها به یکدیگر در همکاری/ارتباطات/قابلیت اطمینان نمره می‌دهند |
| 🪞 **خودارزیابی** | کارمند نمره خود را ثبت می‌کند؛ مقایسه با نمره مدیر در گزارش |
| 🎯 **طرح بهبود (PIP)** | تشخیص خودکار افت متوالی، ایجاد طرح بهبود با وضعیت‌پذیری |
| 📅 **غیبت‌ها** | ثبت روزهای غیبت، نرخ غیبت سال جاری، تفکیک تیمی |
| 📋 **گزارش سفارشی** | فیلتر بر اساس تیم/بازه/معیار/بازه نمره |
| 💾 **پشتیبان‌گیری** | دانلود نسخه پشتیبان SQLite + بازیابی از فایل |
| 📜 **گزارش فعالیت (Audit)** | ثبت خودکار تغییرات (کارمند، نمره، پیکربندی، ورود گروهی) برای انطباق HR |

> **نکته:** این سیستم در حالت فعلی **بدون احراز هویت** است و یک ادمین تمام بخش‌ها را مدیریت می‌کند. اگر در آینده مدیران و کارمندان واقعی وارد شوند، می‌توان نقش‌محور (RBAC) اضافه کرد.

---

## شروع سریع (Docker)

```bash
docker compose up --build -d

# دسترسی:
# فرانت‌اند: http://localhost:3000
# مستندات API: http://localhost:8000/docs
# سلامت:      http://localhost:8000/health
```

### دستورات Docker

```bash
docker compose up --build   # اجرا و ساخت
docker compose up -d        # اجرا در پس‌زمینه
docker compose down         # توقف
docker compose logs -f backend   # لاگ بک‌اند
docker compose logs -f frontend  # لاگ فرانت‌اند
```

**داده‌های SQLite** در حجم `db-data` (مپ‌شده به `/app/data` در کانتینر) نگهداری می‌شوند و با `docker compose down` از بین نمی‌روند. دادهٔ نمونه (تیم‌ها، معیارها و کارمندان) به‌صورت خودکار هنگام اولین اجرا seed می‌شود؛ برای شروع از نو، حجم را حذف کنید: `docker volume rm kpi-calc_db-data`.

### انتشار به GitHub و Docker Hub

اسکریپت `release.sh` همهٔ مراحل (ساخت ریپو، push و تگ‌زدن ایمیج‌ها) را یکجا انجام می‌دهد:

```bash
bash release.sh           # پیش‌فرض: نسخهٔ v1.0.1
bash release.sh v1.1.0    # با نسخهٔ دلخواه
```

قبل از اجرا، یک‌بار وارد حساب‌های خود شوید:

```bash
docker login -u meysam8498        # داکر هاب
gh auth login                      # گیت‌هاب (یا یک Personal Access Token)
```

پس از آن، ایمیج‌ها با نام‌های زیر روی Docker Hub می‌نشینند:

```
meysam8498/hr-kpi-platform:backend-<نسخه>
meysam8498/hr-kpi-platform:frontend-<نسخه>
meysam8498/hr-kpi-platform:backend-latest
meysam8498/hr-kpi-platform:frontend-latest
```

---

## راه‌اندازی توسعه محلی

### بک‌اند

```bash
cd backend
python -m venv venv
# Windows: venv\Scripts\activate   |  Linux/Mac: source venv/bin/activate
pip install -r requirements.txt
uvicorn app.main:app --reload --port 8000
```

### فرانت‌اند

```bash
cd frontend
npm install
npm run dev          # روی پورت 8003 (در package.json تنظیم شده)
```

> اگر `NEXT_PUBLIC_API_URL` تنظیم نشود، فرانت‌اند به `http://localhost:8000` وصل می‌شود.

---

## معماری و جریان داده

```
┌─────────────────────────────────────────────┐
│                 Browser                      │
│  ┌────────────────────────────────────────┐  │
│  │       Next.js Frontend (React)         │  │
│  │   http://localhost:8003 (dev)          │  │
│  └──────────────┬─────────────────────────┘  │
│                 │ /api/*                     │
│  ┌──────────────▼─────────────────────────┐  │
│  │       FastAPI Backend (Python)         │  │
│  │   http://localhost:8000                │  │
│  └──────────────┬─────────────────────────┘  │
│                 │                             │
│  ┌──────────────▼─────────────────────────┐  │
│  │          SQLite Database               │  │
│  │   kpi_system.db (WAL mode)            │  │
│  └────────────────────────────────────────┘  │
└─────────────────────────────────────────────┘
```

**تصمیم‌های کلیدی طراحی:**

- **صفر وابستگی ابری** — همه‌چیز به‌صورت محلی اجرا می‌شود.
- **SQLite با WAL** — سبک، خواندن همزمان، مناسب برای ≤۵۰ کاربر.
- **پیکربندی‌محور** — معیارها، وزن‌ها، دوره‌ها و ترکیب نمره در دیتابیس ذخیره می‌شوند، نه در کد.
- **تک‌ادمین بدون احراز هویت** — ساده‌ترین حالت برای ابزار داخلی.

### جریان داده

1. **ادمین** پیکربندی می‌کند: تیم‌ها، معیارهای KPI (مشترک/تخصصی)، وزن‌دهی هر تیم، دوره‌های گزارش‌گیری، و **ترکیب نمره نهایی**.
2. **نمره‌دهی:** از طریق فرم دستی یا **اکسل** (دانلود فرم ← پر کردن ← آپلود) برای هر کارمند در هر دوره ثبت می‌شود.
3. **موتور KPI** محاسبه می‌کند:
   - نمره پایه = میانگین وزنی معیارها.
   - نمره نهایی = ترکیبِ نمره پایه + ارزیابی ۳۶۰ + تحقق اهداف با وزن‌های هر تیم.
   - نتیجه با جزئیات کامل کش می‌شود (مجددا قابل محاسبه است).
4. **گزارش‌ها** نمایش می‌دهند: تاریخچه فردی، میانگین تیمی، نمای کل شرکت، و داشبورد منابع انسانی.

---

## مرجع API

**Base URL:** `http://localhost:8000` — همه‌ی endpoint ها **بدون احراز هویت** در دسترس‌اند (تک‌ادمین).

### تیم‌ها

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/teams/` | لیست تیم‌ها (با تعداد اعضا) |
| GET | `/api/teams/{id}` | جزئیات تیم |
| POST | `/api/teams/` | ساخت تیم |
| PUT | `/api/teams/{id}` | ویرایش تیم |
| DELETE | `/api/teams/{id}` | حذف تیم |

### کارمندان

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/employees/` | لیست کارمندان فعال (فیلتر `team_id`, `archived`) |
| GET | `/api/employees/all` | همه کارمندان شامل آرشیو |
| GET | `/api/employees/{id}` | جزئیات کارمند |
| POST | `/api/employees/` | ایجاد کارمند |
| PUT | `/api/employees/{id}` | ویرایش کارمند |
| PUT | `/api/employees/{id}/transfer?new_team_id=` | انتقال به تیم دیگر |
| PUT | `/api/employees/{id}/archive` | آرشیو کردن |
| PUT | `/api/employees/{id}/unarchive` | بازگردانی از آرشیو |
| DELETE | `/api/employees/{id}` | حذف کارمند |
| POST | `/api/employees/import-json` | ورود گروهی از JSON |
| POST | `/api/employees/import-excel` | ورود گروهی از اکسل (ستون‌ها: کد، نام، نام‌خانوادگی، سمت، تیم، تاریخ، تلفن) |

### KPI (معیارها / پیکربندی / دوره‌ها / نمره‌دهی / محاسبه / گزارش)

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/kpi/criteria` | لیست معیارهای فعال (`category` اختیاری) |
| POST | `/api/kpi/criteria` | ایجاد معیار |
| PUT | `/api/kpi/criteria/{id}` | ویرایش معیار |
| DELETE | `/api/kpi/criteria/{id}` | غیرفعال‌سازی معیار |
| GET | `/api/kpi/teams/{team}/config` | پیکربندی معیارهای تیم |
| POST | `/api/kpi/teams/{team}/config` | افزودن معیار + وزن |
| PUT | `/api/kpi/teams/{team}/config/{cfg}` | ویرایش وزن |
| DELETE | `/api/kpi/teams/{team}/config/{cfg}` | حذف معیار از تیم |
| GET | `/api/kpi/teams/{team}/blend` | ترکیب نمره نهایی تیم (پایه/۳۶۰/اهداف) |
| PUT | `/api/kpi/teams/{team}/blend` | تنظیم ترکیب نمره (نرمال خودکار به ۱۰۰٪) |
| GET | `/api/kpi/periods` | لیست دوره‌ها |
| POST | `/api/kpi/periods` | ایجاد دوره |
| PUT | `/api/kpi/periods/{id}` | ویرایش/فعال‌سازی دوره |
| POST | `/api/kpi/periods/auto-archive` | آرشیو خودکار دوره‌های >۳ ماه |
| GET | `/api/kpi/entries` | لیست نمرات (`employee_id`, `period_id`) |
| POST | `/api/kpi/entries/batch?employee_id=&period_id=` | ثبت گروهی نمرات |
| POST | `/api/kpi/calculate/{emp}/{period}` | محاسبه نمره یک کارمند |
| POST | `/api/kpi/calculate/team/{team}/{period}` | محاسبه نمره کل تیم |
| POST | `/api/kpi/calculate/company/{period}` | محاسبه نمره کل شرکت |
| GET | `/api/kpi/results/{emp}` | نتایج یک کارمند |
| GET | `/api/kpi/history/{emp}` | تاریخچه یک کارمند |
| GET | `/api/kpi/reports/team/{team}/{period}` | گزارش تیمی |
| GET | `/api/kpi/reports/company/{period}` | گزارش شرکت |
| GET | `/api/kpi/export/{team}/{period}` | دانلود فرم امتیازدهی اکسل |
| POST | `/api/kpi/import/{team}/{period}` | آپلود فرم امتیازدهی پر شده |

### اهداف

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/goals/` | لیست اهداف (`employee_id`, `period_id`) |
| POST | `/api/goals/` | ایجاد هدف |
| PUT | `/api/goals/{id}` | ویرایش / به‌روزرسانی پیشرفت |
| DELETE | `/api/goals/{id}` | حذف هدف |

### ارزیابی ۳۶۰

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/peer-reviews/` | لیست ارزیابی‌ها (`period_id`, `reviewee_id`) |
| POST | `/api/peer-reviews/` | ثبت ارزیابی |
| PUT | `/api/peer-reviews/{id}` | ویرایش ارزیابی |
| DELETE | `/api/peer-reviews/{id}` | حذف ارزیابی |
| GET | `/api/peer-reviews/summary/{reviewee}/{period}` | میانگین ارزیابی‌های یک کارمند |

### خودارزیابی

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/self-evaluations/` | لیست خودارزیابی‌ها |
| POST | `/api/self-evaluations/` | ثبت خودارزیابی |
| PUT | `/api/self-evaluations/{id}` | ویرایش |
| DELETE | `/api/self-evaluations/{id}` | حذف |

### طرح بهبود (PIP)

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/pips/` | لیست PIPها |
| POST | `/api/pips/` | ایجاد PIP |
| PUT | `/api/pips/{id}` | ویرایش وضعیت/یادداشت |
| DELETE | `/api/pips/{id}` | حذف |
| POST | `/api/pips/check-auto` | تشخیص خودکار افت متوالی و ساخت PIP |

### اعلان‌ها

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/notifications/` | لیست اعلان‌ها (`unread_only`) |
| GET | `/api/notifications/unread-count` | تعداد خوانده‌نشده |
| POST | `/api/notifications/` | ایجاد اعلان |
| PUT | `/api/notifications/{id}/read` | علامت‌گذاری خوانده‌شده |
| PUT | `/api/notifications/read-all` | خواندن همه |
| DELETE | `/api/notifications/{id}` | حذف اعلان |
| POST | `/api/notifications/check-deadlines` | تولید یادآوری مهلت دوره‌ها |

### غیبت‌ها

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/absences/` | لیست غیبت‌ها |
| POST | `/api/absences/` | ثبت غیبت |
| DELETE | `/api/absences/{id}` | حذف |
| GET | `/api/absences/summary` | خلاصه غیبت سال جاری (نرخ، تفکیک تیمی) |

### منابع انسانی (داشبورد)

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/hr/analytics` | نیروی کار + توزیع نمرات + رتبه‌بندی + روند + بهبود + غیبت + فاصله خودارزیابی + هشدار افت + تحقق اهداف |

### گزارش سفارشی

| Method | Endpoint | توضیح |
|--------|----------|-------|
| POST | `/api/reports/custom` | گزارش با فیلتر دلخواه (تیم/دوره/معیار/بازه نمره) |

### گزارش فعالیت (Audit) و پشتیبان‌گیری

| Method | Endpoint | توضیح |
|--------|----------|-------|
| GET | `/api/audit-logs/` | لیست لاگ تغییرات (`entity_type`, `action`, `limit`) |
| GET | `/api/backup/info` | اطلاعات فایل دیتابیس |
| GET | `/api/backup/download` | دانلود نسخه پشتیبان SQLite |
| POST | `/api/backup/restore` | بازیابی دیتابیس از فایل آپلودشده |

---

## فرمول محاسبه KPI

### نمره پایه — میانگین وزنی

```
Base Score = Σ(score_i × weight_i) / Σ(weight_i)
```

- `score_i` = نمره مدیر برای معیار i (۰-۱۰۰)
- `weight_i` = وزن اهمیت معیار i (به تفکیک تیم)

### نمره نهایی — ترکیب (Blend)

```
Final Score = (Base×base_w + Peer×peer_w + Goal×goal_w) / (base_w + peer_w + goal_w)
```

| مؤلفه | توضیح | وزن پیش‌فرض |
|-------|-------|-------------|
| **نمره مدیر (KPI)** | میانگین وزنی معیارها | ۷۰٪ |
| **ارزیابی ۳۶۰ (همکاران)** | میانگین سه بعد همکاری/ارتباطات/قابلیت اطمینان | ۱۵٪ |
| **تحقق اهداف** | میانگین درصد تحقق اهداف دوره | ۱۵٪ |

**قواعد:**
- وزن‌ها **به تفکیک تیم** در «تنظیمات KPI → ترکیب نمره نهایی» قابل تنظیم‌اند و هنگام ذخیره خودکار به ۱۰۰٪ نرمال می‌شوند.
- فقط مؤلفه‌هایی که داده دارند در محاسبه وارد می‌شوند (اگر کارمندی ارزیابی ۳۶۰ و هدف نداشته باشد، نمره‌اش با نرمال‌سازی وزن‌های موجود محاسبه می‌شود تا به‌طور مصنوعی پایین نیاید).

### مثال

**علی رضایی — تیم نرم‌افزار** (وزن‌ها: پایه ۰.۷، ۳۶۰ ۰.۱۵، اهداف ۰.۱۵)

| مؤلفه | نمره | وزن | سهم |
|-------|------|-----|-----|
| نمره مدیر (KPI) | ۸۸.۴ | ۰.۷ | ۶۱.۸۸ |
| ارزیابی ۳۶۰ | ۹۰.۰ | ۰.۱۵ | ۱۳.۵۰ |
| تحقق اهداف | ۹۶.۴۳٪ | ۰.۱۵ | ۱۴.۴۶ |
| **نمره نهایی** | | | **۸۹.۸۴** |

#### منابع

- Kaplan, R.S. & Norton, D.P. (1996). *The Balanced Scorecard*. Harvard Business School Press.
- Armstrong, M. & Taylor, S. (2020). *Armstrong's Handbook of HRM Practice*. Kogan Page.
- SHRM. Best Practices in Performance Management.

---

## راهنمای پیکربندی KPI

### معیارهای پیش‌فرض

**مشترک (همه تیم‌ها):** حضور و غیاب، مهارت‌های ارتباطی، همکاری تیمی، حل مسئله، انعطاف‌پذیری.

**تخصصی (مثال‌ها):** کیفیت کد، سرعت تحویل، نوآوری فنی (نرم‌افزار/زیرساخت)؛ دستیابی به هدف فروش، روابط با مشتری (فروش)؛ رعایت فرآیندها (HR)؛ دقت مالی (مالی).

### افزودن معیار جدید
1. به **⚙️ تنظیمات KPI** بروید.
2. در «معیارهای KPI» → «+ معیار جدید» نام، توضیح و دسته (مشترک/تخصصی) را وارد کنید.
3. «ذخیره» را بزنید.

### تغییر وزن‌دهی هر تیم
1. در «وزن‌دهی معیارها» تیم را انتخاب کنید.
2. معیار و وزن درصد را اضافه یا حذف کنید (مجموع = ۱۰۰٪ توصیه می‌شود).

### تنظیم ترکیب نمره نهایی
1. در «🧬 ترکیب نمره نهایی» تیم را انتخاب کنید.
2. سه وزن (نمره مدیر / ارزیابی ۳۶۰ / تحقق اهداف) را به درصد تنظیم کنید.
3. «ذخیره ترکیب» — وزن‌ها به‌صورت خودکار نرمال می‌شوند و تغییر در لاگ فعالیت ثبت می‌شود.

### ساخت دوره گزارش‌گیری
1. به **📅 دوره‌ها** بروید.
2. نام، نوع (ماهانه/فصلی/…)، تاریخ شروع و پایان را وارد کنید.
3. «ایجاد» — دوره فعال می‌شود و می‌توان برای آن نمره‌دهی کرد. دوره‌های بیش از ۳ ماه از پایان، خودکار آرشیو می‌شوند.

---

## تست

### بک‌اند

```bash
cd backend
pip install pytest httpx
python -m pytest tests/ -v
```

تست‌ها شامل:
- **موتور KPI**: فرمول میانگین وزنی، ترکیب با ارزیابی ۳۶۰ و اهداف، نرمال‌سازی وزن‌ها، موارد مرزی
- **API (بدون احراز هویت)**: CRUD کارمند، انتقال/آرشیو، ورود گروهی، تنظیم ترکیب نمره، لاگ فعالیت، پشتیبان‌گیری

---

## ساختار پروژه

```
kpi-system/
├── docker-compose.yml          # ارکستراسیون کامل
├── README.md
├── backend/
│   ├── Dockerfile
│   ├── requirements.txt
│   ├── app/
│   │   ├── main.py             # FastAPI app + CORS + seed داده
│   │   ├── config.py           # تنظیمات از env
│   │   ├── database.py         # موتور SQLAlchemy + session
│   │   ├── models.py           # مدل‌های ORM
│   │   ├── schemas.py          # اسکیمای Pydantic
│   │   ├── kpi_engine.py       # موتور محاسبه KPI (ترکیب وزنی)
│   │   ├── excel.py            # خروجی/ورودی اکسل
│   │   └── routes/
│   │       ├── teams.py        # تیم‌ها
│   │       ├── employees.py    # کارمندان + ورود گروهی
│   │       ├── kpi.py          # معیار/وزن/ترکیب/دوره/نمره/گزارش
│   │       ├── goals.py        # اهداف
│   │       ├── peer_reviews.py # ارزیابی ۳۶۰
│   │       ├── self_evaluations.py
│   │       ├── pips.py         # طرح بهبود
│   │       ├── notifications.py
│   │       ├── absences.py     # غیبت‌ها
│   │       ├── custom_reports.py
│   │       ├── hr_analytics.py # داشبورد منابع انسانی
│   │       ├── audit.py        # گزارش فعالیت
│   │       └── backup.py       # پشتیبان‌گیری/بازیابی
└── frontend/
    ├── Dockerfile
    ├── package.json
    ├── next.config.ts          # output: standalone + rewrite /api
    └── src/
        ├── app/                # صفحات (dashboard, scoring, employees, teams, goals, absences, peer-reviews, pips, self-eval, reports, custom-reports, excel, hr, notifications, audit, backup, admin/config, admin/periods)
        ├── components/         # Layout, NotificationBell, JalaliDatePicker, DesignerCard
        └── lib/
            ├── api.ts          # کلاینت API + تایپ‌ها
            ├── jalali.ts       # تبدیل تاریخ شمسی
            └── theme-context.tsx
```

---

## 👨‍💻 طراح سیستم

| | |
|---|---|
| **نام** | میثم ایجادی (Meysam Ijadi) |
| **نقش** | طراح سیستم |
| **گیت‌هاب** | https://github.com/meysam8498 |
| **داکر هاب** | https://hub.docker.com/u/meysam8498 |
| **لینکدین** | https://linkedin.com/in/meysam-ijadi-920817127/ |
| **تلگرام** | https://t.me/Meysam_Ijadi |
| **واتساپ** | https://wa.me/Meysam_Ijadi |

پروفایل طراح در پایین سایدبار (و منوی موبایل) برنامه نیز نمایش داده می‌شود.

---

## مجوز

استفاده داخلی. ساخته‌شده برای سازمان‌هایی که به یک راه‌حل ساده و محلی مدیریت KPI نیاز دارند.
