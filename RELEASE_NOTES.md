# v1.7.0 — انتشار عمومی، لایسنس و اصلاحات رابط کاربری

**تاریخ:** ۱۴۰۵/۰۶/۱۷

## خلاصه
نسخه‌ای که پروژه را برای انتشار عمومی آماده می‌کند: لایسنس MIT، مستندات انتشار، حذف کامل نام ابزارهای توسعه از کد/گیت/داکر، و چند اصلاح رابط کاربری در داشبورد.

## تغییرات

### ✨ قابلیت‌های جدید
- افزودن لایسنس MIT (`LICENSE`).
- افزودن فایل‌های مشارکت (`CONTRIBUTING.md`)، رفتار (`CODE_OF_CONDUCT.md`) و امنیت (`SECURITY.md`).
- افزودن راهنمای کامل انتشار (`PUBLISHING.md`) و قالب یادداشت Release (`RELEASE_NOTES.md`).
- افزودن متادیتای OCI به ایمیج‌های داکر (عنوان، نویسنده، لایسنس، لینک منبع).

### 🎨 رابط کاربری
- هم‌تراز شدن «دوره فعال» و دکمه «یادآوری» در یک ردیف در داشبورد.
- خلاصه‌سازی لیست کارمندان داشبورد به ۸ نفر با دکمه «نمایش همه».
- به‌روزرسانی خودکار نسخه و تاریخچه در صفحه «درباره برنامه».

### 🔧 فنی
- حذف نام ابزارهای توسعه از کد، تاریخچه گیت و متادیتای داکر.
- هماهنگ‌سازی خودکار `APP_VERSION` با تگ Release.

## نصب / اجرا

```bash
docker compose up -d --build
```

---

## قالب برای Releaseهای بعدی

از این ساختار برای هر Release جدید استفاده کنید:

```markdown
# vX.Y.Z — <عنوان>

**تاریخ:** ۱۴۰۵/... 

## خلاصه
<یک پاراگراف درباره هدف این نسخه>

## تغییرات

### ✨ قابلیت‌های جدید
- ...

### 🎨 رابط کاربری
- ...

### 🔧 فنی
- ...

### 🐛 رفع باگ
- ...

## نصب / اجرا

```bash
docker compose up -d --build
```
```
