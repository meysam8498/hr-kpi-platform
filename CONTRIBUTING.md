# راهنمای مشارکت در توسعه

از علاقه شما به بهبود این پروژه سپاسگزاریم. این راهنما مسیر مشارکت را روشن می‌کند.

## قوانین کلی

- کد را تمیز، خواناتر و هماهنگ با سبک موجود نگه دارید.
- پیش از ارسال، تایپ‌چک و تست‌ها را اجرا کنید.
- از `git commit` پیام‌های کوتاه و توصیفی استفاده کنید (ترجیحاً `type: subject`).

## شروع کار

```bash
# بک‌اند
cd backend
pip install -r requirements.txt
python -m pytest tests/ -v

# فرانت‌اند
cd frontend
npm install
npx tsc --noEmit
npm run dev
```

## ساختار پروژه

```
backend/      — FastAPI، SQLite، Alembic، موتور محاسبه KPI، گزارش‌ها (PDF/Excel)
frontend/     — Next.js، رابط کاربری فارسی، تم دارک/لایت
docker-compose.yml — استک کامل لوکال
release.sh    — انتشار خودکار (گیت‌هاب + داکر هاب)
```

## شاخه‌ها

- از شاخه `main` برای توسعه مستقیم استفاده کنید.
- برای تغییرات بزرگ، یک شاخه اختصاصی بسازید و بعد از تست، merge کنید.

## تست و کیفیت

- برای منطق حساس (محاسبه KPI، سطوح دسترسی، اعتبارسنجی) تست بنویسید.
- پیش از هر Release، `pytest` و `tsc --noEmit` باید سبز باشند.

## لایسنس

با ارسال تغییرات، موافقت می‌کنید که کد شما تحت **MIT** منتشر شود.
